{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "description": "A unique identifier for the configuration entry."
        },
        "deviceId": {
            "type": "string",
            "description": "A reference to the associated device."
        },
        "key": {
            "type": "string",
            "description": "The configuration key that specifies what setting is being configured."
        },
        "value": {
            "type": "string",
            "description": "The value associated with the configuration key."
        }
    },
    "required": [
        "id",
        "deviceId",
        "key",
        "value"
    ],
    "additionalProperties": false
}